[package]
name = "tidebreak-desktop"
description = "Tidebreak desktop: the Tauri application shell that compiles the core in and hosts the chat UI."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[lints]
workspace = true

[lib]
name = "tidebreak_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]
# This crate has no executable documentation examples. Its behavior contracts
# live in the library test suite.
doctest = false

[[bin]]
name = "tidebreak-desktop"
path = "src/main.rs"
# The binary is only a four-line launcher into the tested library. The
# all-targets clippy lane still compiles it on every Rust change.
test = false

[build-dependencies]
tauri-build = { version = "=2.6.3", features = [] }
serde_json = { workspace = true }

[features]
default = ["tidebreak-server/keychain"]

[dependencies]
async-trait.workspace = true
base64.workspace = true
chrono = { workspace = true }
cap-fs-ext.workspace = true
cap-std.workspace = true
# The per-user folders Tauri names an app's data, local data, and settings
# folders from, resolved the same way before the app is built, so the move
# from the previous app identity lands where Tauri then looks. Already in
# Cargo.lock through Tauri.
dirs = "=6.0.0"
futures.workspace = true
flate2 = "=1.1.10"
minisign-verify.workspace = true
tidebreak-code-execution = { path = "../tidebreak-code-execution" }
tidebreak-core = { path = "../tidebreak-core", default-features = false, features = ["keychain"] }
tidebreak-harness = { path = "../tidebreak-harness" }
tidebreak-host-broker = { path = "../tidebreak-host-broker" }
tidebreak-managed-node = { path = "../tidebreak-managed-node" }
tidebreak-server = { path = "../tidebreak-server-api" }
reqwest = { workspace = true }
semver = "=1.0.28"
sha2 = { workspace = true }
symphonia = { workspace = true }
opus-decoder = { workspace = true }
tauri = { version = "=2.11.6", features = ["unstable"] }
tauri-plugin-deep-link = "=2.4.10"
tauri-plugin-dialog = "=2.7.3"
tauri-plugin-notification = "=2.4.0"
tauri-plugin-shell = "=2.3.6"
tauri-plugin-updater = "=2.12.0"
tauri-plugin-window-state = "=2.4.1"
serde = { workspace = true }
serde_json = { workspace = true }
thiserror = { workspace = true }
tokio = { workspace = true }
tokio-util = { workspace = true }
# The sidecar's stderr and the quit flow's warnings go to the shared log the
# embedded server installs.
tracing = { workspace = true }
unicode-general-category = { workspace = true }
url = { workspace = true }
uuid = { workspace = true }
tempfile.workspace = true
tar = "=0.4.46"
zip.workspace = true


[dev-dependencies]
tauri = { version = "=2.11.6", features = ["test"] }

[target.'cfg(any(target_os = "macos", target_os = "linux", windows))'.dependencies]
tauri-plugin-single-instance = { version = "=2.4.5", features = ["deep-link"] }

[target.'cfg(windows)'.dependencies]
windows-sys = { workspace = true, features = ["Win32_Foundation", "Win32_Security", "Win32_Security_Authorization", "Win32_Storage_FileSystem", "Win32_System_SystemServices"] }

[target.'cfg(unix)'.dependencies]
libc.workspace = true

[target.'cfg(target_os = "macos")'.dependencies]
block2 = "=0.6.2"
objc2 = "=0.6.4"
objc2-app-kit = { version = "=0.3.2", default-features = false, features = ["NSAccessibility", "NSAccessibilityProtocols", "NSApplication", "NSBezierPath", "NSColor", "NSEvent", "NSGraphics", "NSGraphicsContext", "NSPanel", "NSResponder", "NSRunningApplication", "NSScreen", "NSText", "NSView", "NSWindow", "libc", "objc2-core-graphics"] }
objc2-core-graphics = { version = "=0.3.2", default-features = false, features = ["CGEvent", "CGEventSource", "CGEventTypes", "CGGeometry", "CGWindow"] }
objc2-foundation = { version = "=0.3.2", default-features = false, features = ["NSArray", "NSBundle", "NSDictionary", "NSError", "NSGeometry", "NSKeyValueObserving", "NSString", "NSURL", "NSURLRequest", "NSUUID", "NSValue"] }
objc2-web-kit = { version = "=0.3.2", default-features = false, features = ["WKContentWorld", "WKFrameInfo", "WKNavigation", "WKNavigationAction", "WKOpenPanelParameters", "WKSecurityOrigin", "WKUIDelegate", "WKWebView", "WKWebViewConfiguration", "WKWindowFeatures", "block2", "objc2-app-kit"] }
plist = "=1.10.1"
