# Native-only Tauri commands.
#
# Decision record 7 (docs/decisions/0007-cli-headless-feature-parity.md) makes
# the server HTTP/WS API the canonical product surface: a feature has
# CLI/headless parity when it is reachable through `tidebreak-server` routes.
# A `#[tauri::command]` is reserved for what genuinely needs the native shell —
# OS file/folder pickers, keychain and code-signing plumbing, the auto-updater,
# native window/attention control, the office-converter and Node runtime
# installers, local voice capture, and debug-bundle save dialogs. Anything else
# arriving as a Tauri command is a parity bug.
#
# Every `#[tauri::command]` in `crates/tidebreak-desktop/src/` must appear here
# with a one-line justification, and every line here must still name a command
# in the source. `command_parity::allowlist_covers_every_tauri_command` enforces
# both directions.
#
# Adding a line is a conscious amendment, not a formality: if the command is not
# native-only, it belongs behind a server route instead. Lines marked
# `TODO(record 7)` are commands that already existed when the gate landed and do
# not fit the closed native-only set — they are recorded debt to migrate, not
# precedent for new ones.
#
# Format: one `name: justification` per line; `#` starts a comment.

# Shell bootstrap and native window control
server_info: hands the renderer the embedded server's address and per-launch token — the handshake that makes every HTTP route reachable, so it cannot itself be one.
retry_server_boot: runs the embedded server's boot again after it failed before binding; the server it would ask is the one that is not running.
local_boot_failure: says which known failure stopped the embedded server and where its data folder is, for the boot screen; the server that failed cannot answer for itself.
request_user_attention: native window attention hint when a parked question needs the user.
is_window_focused: reports main-window focus; document.hidden is the tab, not the window.
present_native_notification: posts an OS banner and asks for notification permission on first unfocused present.
code_browser_import_legacy_state: moves one renderer browser record into private native storage and acknowledges disposal before the renderer deletes its old copy.
code_browser_command: creates and controls sandboxed native child webviews whose bounds and lifecycle exist only in the desktop shell.
code_browser_agent_tabs: read live independent native webview identities so the desktop can restore inactive previews after its renderer remounts; a headless server has no native views.
open_code_worktree: opens a local Code worktree through this operating system's folder handler; a headless server cannot open desktop UI.
open_in_editor: starts the editor installed on the machine the reader is sitting at, on one file inside a local worktree; the server may be that machine or another, and neither can raise a desktop editor here.
detect_external_editors: probes this computer's PATH and application folders for installed editors, which only the client machine can see.

# Attaching this client to a machine it does not host (ADR 0047)
remote_machine_state: reports which machine this client is attached to, from shell-held state that exists before any machine is reachable.
connect_remote_machine: stores the machine token in the OS credential store, which only the native shell can reach.
connect_gateway_remote_machine: mints a machine-bound token from the shell's keychain-held Gateway OAuth session (ADR 0049); the rotating refresh credential never leaves the native shell.
remote_machine_access_token: refreshes that machine-bound token from the same keychain-held session so HTTP calls and new sockets use the latest value.
disconnect_remote_machine: removes that stored token from the OS credential store.

# OS pickers, drops, and save dialogs
pick_code_directory: native directory picker for code-mode repo registration and clone destinations; grant-free — the path is handed to the server for validation (ADR 0030).
attach_chat_files: opens the OS file picker; only the native shell can turn a user's file choice into readable paths.
attach_dropped_chat_files: claims the operating-system drop this window received and routes the dropped paths through the same ingest.
export_library_document: writes a source document's bytes wherever the native save dialog sends them.
export_deliverable: writes an output revision to a destination chosen in the native save dialog.
save_chat_debug_bundle: writes the untruncated debug bundle to a file chosen in the native save dialog.
save_workspace_config: writes a portable workspace configuration to a file chosen in the native save dialog.
pick_workspace_config: opens a portable workspace configuration through the native file dialog.
save_profile_backup: asks the native save dialog where a backup goes and streams POST /data/backup there; the archive itself is the server's.
save_conversation_export: asks the native save dialog where an export goes and streams POST /data/export there; the export itself is the server's.
reveal_data_directory: opens this computer's data folder in its file manager; a headless server cannot open desktop UI.
copy_chat_debug_bundle: returns the clipboard-sized bundle on the same user gesture the webview requires for a clipboard write.
import_skills: opens the native folder picker and copies validated local skill packages into this desktop profile.

# Host folder consent that runs through native pickers and permission dialogs
connect_folder: native folder picker — the host consent path for attaching a folder to a chat.
grant_folder_capability: widens a folder's capability only after the native permission dialog is confirmed.

# Host-side execution of client-executed tool calls
resolve_folder_access_request: carries the user's in-turn folder decision into the native picker and the host broker that owns the grant.
resolve_output_writeback_request: executes the approved write into a connected host folder, which only the desktop-side broker can reach.
put_native_mcp_servers: shows the OS-native local-command warning and uses the host-only executor credential to save an approved configuration.
apply_native_workspace_config: shows the same OS-native local-command warning for an import that would start local MCP commands, and uses the host-only executor credential to apply the approved import.

# Deleting this computer's profile
delete_all_data: asks in a native dialog, stops the server and its workers, removes this channel's keychain items, the code browser's website data, and the app's own folders, then quits the process so nothing writes into a half-deleted profile; keychain plumbing, WebKit data stores, and the app's lifecycle are the shell's.

# Gateway pairing, which no HTTP route may reach (docs/gateway-boundary.md)
leave_provisioned_gateway: deletes this computer's provisioned gateway policy through the embedded server's pairing handle; policy writes are deliberately kept off every HTTP route.

# Computer-use control indicator, Stop, and native permission setup
computer_use_permission_status: checks this desktop's native helper permissions and running bundle identity, which macOS uses to attribute screen recording and accessibility access.
request_computer_use_permissions: asks macOS for screen recording and accessibility permission through the running desktop's native helper on an explicit user gesture.
open_computer_use_permission_settings: opens a fixed local macOS System Settings pane for screen recording or accessibility permission.
computer_use_state: reads the native control state (active app and halt latch) that lives only in this process.
stop_computer_use_control: sets the native halt latch that short-circuits control ops before their next broker round-trip.
resume_computer_use_control: clears that latch on an explicit user gesture.

# The tidebreak command on this computer's PATH
cli_command_status: reads which links to the app bundle's own CLI sidecar exist in this computer's PATH folders and what a login shell on this computer resolves; a server cannot see the client machine's bundle, home folder, or shell.
install_cli_command: links the CLI sidecar inside this app bundle into ~/.local/bin, or into /usr/local/bin through the macOS administrator prompt; only the native shell knows where its bundle is and can raise that prompt.
uninstall_cli_command: removes only a link to a Tidebreak app bundle from those folders, through the same administrator prompt for /usr/local/bin.

# Office converter and Node runtime installers
convert_office_to_pdf: drives the user's locally installed LibreOffice inside the desktop sandbox.
install_presentation_converter: installs the local office converter onto this machine.
cancel_presentation_converter_install: cancels that local installation.
warm_presentation_converter: warms the locally installed converter process.
install_node_runtime: installs the local Node runtime the sandbox image expects.

# Auto-updater
desktop_update_state: reports the native auto-updater's state.
check_for_update: asks the native auto-updater to check.
restart_for_update: relaunches the app to apply a staged native update.
download_update: asks the native auto-updater to download a found update when automatic downloads are off.
desktop_update_preferences: reports whether the native auto-updater downloads on its own, and whether policy locks it.
set_automatic_update_downloads: turns the native auto-updater's automatic downloads on or off.

# Quitting and crash recovery of the desktop process itself
quit_prompt_state: reports the quit prompt the shell is holding the app's exit on; only the native shell receives the operating system's quit request.
quit_prompt_opened: tells the shell the renderer is showing its quit prompt, so it does not fall back to a native dialog.
answer_quit_prompt: carries the person's answer to the shell, which alone can stop, park, or keep the process and then exit it.
restart_app: quits through that same prompt and opens the app again, so macOS applies a Screen Recording grant it only gives to a process started after it; only the native shell can end and relaunch itself.
unclean_exit_notice: reports that the previous desktop process ended without its exit handler, which only the shell's own run marker records.
dismiss_unclean_exit_notice: clears that notice for this run.
data_move_notice: reports that the shell moved this install's data from the previous app identity before the server started, which only the shell's move record says.
dismiss_data_move_notice: marks that notice read in the move record, so it shows once.
save_diagnostics_report: writes the diagnostics export to a file chosen in the native save dialog.
problem_report_facts: reads this computer's operating system version and architecture for a bug report's prefill; only the native shell knows the machine it runs on.
reveal_logs_directory: opens this computer's logs folder in its file manager; a headless server cannot open desktop UI.

# TODO(record 7): commands that predate the gate and do not fit the native-only
# set. Each should become a server route with a CLI client; until then they are
# listed so the gate stays honest about what is already owed.
publish_chat_image: TODO(record 7) — publishes pasted image bytes from the renderer; no native shell involvement, belongs on the ingest routes.
publish_code_image: TODO(record 7) — same as publish_chat_image for a code session; desktop mounts the publish route behind the executor token.
list_connected_folders: TODO(record 7) — broker read with no native UI; headless consumers need the same listing.
list_approved_folders: TODO(record 7) — broker read with no native UI.
attach_trusted_folders: TODO(record 7) — applies this desktop profile's saved folder defaults to a new chat.
set_trusted_folder: TODO(record 7) — stores this desktop profile's automatic folder attachment defaults.
connect_approved_folder: TODO(record 7) — attaches an already approved broker root without a native dialog.
list_capability_consents: TODO(record 7) — broker read with no native UI; the consent projection should be a route.
revoke_capability_consent: TODO(record 7) — broker mutation with no native dialog; revocation must be reachable headlessly.
disconnect_folder: TODO(record 7) — broker mutation with no native dialog; record 7 item 4 gives folders a CLI path.
forget_folder: TODO(record 7) — broker mutation with no native dialog.
purge_deleted_conversation_subject: TODO(record 7) — broker cleanup with no native dialog.
